import { Injectable } from '@nestjs/common';
@Injectable()
export class FilesService {
  async findOneById(id) {
  }

  async uploadFile(fileName: string) {
  }

  async findOne(fileName: number) {
  }

  async update(fileName: string) {
  }
}
